syntax = "proto3";

service Benchmark {
    rpc Enviar(Mensagem) returns (Confirmacao);
}

message Mensagem {
    bytes carga_util = 1;
}

message Confirmacao {
    int32 tamanho_bytes = 1;
    string timestamp = 2;
}
